import { ClearTextPipe } from './clear-text-pipe';

describe('ClearTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ClearTextPipe();
    expect(pipe).toBeTruthy();
  });
});
